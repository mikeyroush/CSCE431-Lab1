<%= link_to "Back to List", books_path, class: 'back-link' %>

<div class="book_edit">
    <h2>Update Book</h2>

    <%= form_for( @book ) do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <div class="form-buttons">
        <%= f.submit "Update Book", disable_with: 'Updating Book...' %>
    </div>
    <% end %>

</div>